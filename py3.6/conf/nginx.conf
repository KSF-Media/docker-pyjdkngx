server {
    location / {
        try_files $uri @app;
    }
    location @app {
        include uwsgi_params;
        uwsgi_buffers 16 8k;
        uwsgi_pass unix:///tmp/uwsgi.sock;
    }
    location /static {
        alias /app/subscribe/static;
    }
}
